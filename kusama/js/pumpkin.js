
window.onload = function init() {
  let Height = window.innerHeight;
  let Width = window.innerWidth;
  document.getElementById('wrap').style.height = Height * 2 + 'px';
  document.getElementById('wrap').style.width = Width + 'px';
}